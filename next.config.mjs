
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/quiz-app-NEXT',
  assetPrefix: '/quiz-app-NEXT/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;